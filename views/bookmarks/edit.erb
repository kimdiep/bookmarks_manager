<%# Our user needs to see the current data in the edit form.
To do this, we need to pass more than just @bookmark_id to the form. We need to pass a Bookmark object that wraps the data for that bookmark. %>

<form action="/bookmarks/<%= @bookmark.id %>" method="post">
<%# Again, we are using the method-override for patch since html doesn't have this automatically available to be defined %>
  <input type="hidden" name="_method" value="PATCH" />
  <input type="text" name="url" value="<%= @bookmark.url %>" />
  <input type="text" name="title" value="<%= @bookmark.title %>" />
  <input type="submit" value="Submit" />
</form>